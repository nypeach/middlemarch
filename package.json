{
  "name": "middlemarch",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd server && nodemon index.js",
    "build": "cd client && npm install",
    // "build": "cd client && npm install && npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "^14.15.4"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.7"
  }
}
